-------------------------------------------------
-- Utility plugins
-------------------------------------------------
-- Little functions here and there

local M = {
    -- Auto create parent directories when saving if they don't exist
    { "jghauser/mkdir.nvim", },
    -- Use :Bdelete to delete a buffer without closing a window
    { "famiu/bufdelete.nvim", },
}

return M
